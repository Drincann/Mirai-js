const { Bot } = require('./Bot');
const { Message } = require('./Message');
const { Middleware } = require('./Middleware');

module.exports = {
    MiraiJs: {
        Bot,
        Message,
        Middleware,
    },
}; 